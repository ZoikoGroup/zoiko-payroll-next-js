import React from "react";
import SectionShell from "./SectionShell";

export default function ComplaintsEscalationSection() {
  return (
    <SectionShell
      id="complaints"
      title="Complaints & escalation"
      intro="If you've reported a barrier and it remains unresolved, you can request escalation through the same feedback form — reference your original case, and ask for escalation review. We'll confirm receipt and next steps."
    />
  );
}
